spring.application.name=smart-bms-backend

# ---------------------------
# 🧠 Server configuration
# ---------------------------
# لوکال: به صورت پیش‌فرض 7080، در Render از SERVER_PORT استفاده می‌کند
server.port=${SERVER_PORT:7080}

# ---------------------------
# 🧠 Database (H2)
# ---------------------------
spring.datasource.url=${DB_URL:jdbc:h2:mem:smartbmsdb}
spring.datasource.driverClassName=${DB_DRIVER:org.h2.Driver}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}

# برای ساخت جداول خودکار و نمایش کوئری‌ها در کنسول
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ---------------------------
# 🧠 H2 console (لوکال روشن، Render خاموش)
# ---------------------------
spring.h2.console.enabled=${H2_CONSOLE_ENABLED:true}
spring.h2.console.path=/h2-console

# ---------------------------
# 🧠 CORS (Frontend URLs)
# ---------------------------
# در لوکال: فرانت روی localhost:8080
# در Render: فرانت ورسل یا آدرس دامنه‌ی فرانت واقعی
frontend.url=${FRONTEND_URL:http://localhost:8080,https://vue-green-five.vercel.app}
