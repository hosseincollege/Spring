spring.application.name=smart-bms-backend

# 🧠 استفاده از متغیر SERVER_PORT از .env برای پورت سرور
server.port=${SERVER_PORT}

# 🧠 استفاده از متغیرهای H2 از .env برای تنظیمات دیتابیس موقت H2
spring.datasource.url=${DB_URL}
spring.datasource.driverClassName=${DB_DRIVER}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# 🎯 تنظیمات JPA
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# فعال‌سازی کنسول مرورگر برای بررسی جداول (استفاده از متغیر از .env)
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console
